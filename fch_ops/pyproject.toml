[project]
name = "fch_ops"
version = "0.1.0"
description = "GreenRay custom operations app for ERPNext v16"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "GPL-3.0" }
authors = [
    { name = "GreenRay / FCH", email = "ops@greenrayleed.com" }
]
dependencies = [
    "frappe",
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
