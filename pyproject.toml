[project]
name = "fch_ops"
version = "0.4.0"
description = "GreenRay regional operations, five-gate sales control, compliance and COMEX extensions for ERPNext v16"
readme = "README.md"
requires-python = ">=3.14"
license = { text = "GPL-3.0-or-later" }
authors = [
    { name = "GreenRay / FCH", email = "ops@greenrayleed.com" }
]
dependencies = []
dynamic = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=16.21.0,<17.0.0"
