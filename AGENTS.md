# GreenRay ERP agent context

Use ERPNext v16 and keep GreenRay custom behavior inside `fch_ops`. Preserve the distinction between legal company, market, stock ownership and physical warehouse. Use the repository documentation as the implementation source of truth.
